---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div
  class:list={[
    "rounded-xl border border-border bg-background p-6 shadow-sm transition-all duration-150 hover:shadow-md",
    className,
  ]}
>
  <slot />
</div>
